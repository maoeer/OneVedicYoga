export interface IPageData {
  email: string;
  code: string;
  password: string;
  confirmPassword: string;
}

export interface IPageTheme {
  title: string;
  subTitle: string;
  confirmText: string;
  linkForeword: string;
  linkText: string;
}